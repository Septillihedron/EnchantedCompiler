
declare module 'https://cdn.jsdelivr.net/npm/yaml@2.3.4/+esm' {
  export function parse(str: string): any;
  export function stringify(obj: any): string;
  const defaultExport: {
    parse: typeof parse;
    stringify: typeof stringify;
  };
  export default defaultExport;
}
